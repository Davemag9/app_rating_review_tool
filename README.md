# App Rating Review Tool

Collect random Google Play reviews with `google-play-scraper`.

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python collect_reviews.py genesis.nebula
```

Output: `data/reviews.json`
